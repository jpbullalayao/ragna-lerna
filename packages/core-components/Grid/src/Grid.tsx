import styled from 'styled-components';

import {
  background,
  border,
  color,
  grid,
  layout,
  position,
  shadow,
  space,
  typography,
} from 'styled-system';

export const Grid = styled.div`
  display: grid; // Default display

  ${background}
  ${border}
  ${color}
  ${grid}
  ${layout}
  ${position}
  ${shadow}
  ${space}
  ${typography}
`;
