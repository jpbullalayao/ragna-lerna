import styled from "styled-components";

import {
  border,
  flexbox,
  grid,
  layout,
  position,
  shadow,
  space,
} from 'styled-system';

export const Image = styled.img`
  ${border}
  ${flexbox}
  ${grid}
  ${layout}
  ${position}
  ${shadow}
  ${space}
`;
